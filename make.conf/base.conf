# Gentoo Portage Configuration
# Base Configuration

# Language Selection
L10N="en_US en-US en"
LINGUAS="en_US en-US en"

# C/C++ Compiler Options
CFLAGS="-march=native -O2 -pipe"
CXXFLAGS="${CFLAGS}"
FFLAGS="${CFLAGS}"
FCFLAGS="${FFLAGS}"

# Portage Features
EMERGE_DEFAULT_OPTS="--with-bdeps=y --dynamic-deps=n"
FEATURES="splitdebug -config-protect-if-modified"

# License Acceptance
ACCEPT_LICENSE="*"

# Testing Branch
ACCEPT_KEYWORDS="~amd64"

# Logging
PORTAGE_ELOG_CLASSES="warn error log"
PORTAGE_ELOG_SYSTEM="echo save"

# CHOST
CHOST="x86_64-pc-linux-gnu"
