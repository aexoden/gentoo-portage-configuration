# Gentoo Portage Configuration
# Package Mask

# Masks listed in this file are to either temporarily mask software that does
# not build or to temporarily restrict versions to avoid annoying messages about
# skipped packages during world upgrades.

# ZFS does not currently support the 6.4 kernel series.
>=sys-kernel/gentoo-kernel-6.4
>=virtual/dist-kernel-6.4

# CUDA does not support greater than GCC 12.2
>=sys-devel/gcc-12.3

# No compatible gst-plugins-libnice
>=net-libs/libnice-0.1.21

# New fchmod wrapper breaks Julia and anything that uses Julia.
# https://bugs.gentoo.org/909435
>=sys-apps/sandbox-2.34
